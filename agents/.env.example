GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_LOCATION=us-central1
GOOGLE_GENAI_USE_VERTEXAI=TRUE
# Elasticsearch data-plane host (the .es. host), e.g. https://<id>.es.<region>.gcp.cloud.es.io
ELASTIC_ENDPOINT=
ELASTIC_API_KEY=
# Kibana host (the .kb. host, NOT the .es. host) — required by elastic/agent_builder/scripts/upsert_{tool,workflow}.sh
KIBANA_URL=