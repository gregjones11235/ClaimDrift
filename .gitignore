# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Windows zone identifier sidecar files
*Zone.Identifier
*:Zone.Identifier

# ADK local session/state
.adk/
*.adk/

# IDE / OS
.DS_Store
.idea/
.vscode/

# Secrets / local env
.env
.env.*
!.env.example

# Elastic Workflows with inlined bearer tokens — keep only the
# .template.yaml versions in git (T2; Elastic 9.3 has no secret store).
elastic/agent_builder/workflows/dispatch_new_pairs.yaml

# tmp files
.tmp*

# tool scripts
/scripts/